import { ApiError } from '../platform';
declare type ErrorInfo = Omit<ApiError, 'ok'>;
/**
 * This class represents errors that are thrown by grammY because the Telegram
 * Bot API responded with an error.
 *
 * Instances of this class hold the information that the Telegram backend
 * returned.
 */
export declare class GrammyError extends Error implements ErrorInfo {
    readonly payload: object;
    readonly error_code: number;
    readonly description: string;
    constructor(message: string, info: ErrorInfo, payload: object);
}
export {};
